# Production Environment Configuration

# Application Settings
APP_NAME=Secrets Management Demo (PROD)
APP_ENV=production
DEBUG=false
LOG_LEVEL=error

# Non-sensitive configuration
PUBLIC_API_ENDPOINT=https://api.example.com
PUBLIC_API_KEY=prod_api_key_67890

# Even in a .env.prod file, you should not store sensitive information
# Use Docker secrets or a secrets manager in production 