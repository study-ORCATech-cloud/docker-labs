version: '3.8'

services:
  web:
    # TODO: Build the web service
    # HINT: Use the build directive to point to ./app
    # TODO: Configure ports for the web service
    # HINT: Map port 8000 to container port 8000
    # TODO: Configure environment variables for the web service
    # HINT: Set REDIS_HOST to redis and REDIS_PORT to 6379
    # TODO: Configure volumes for the web service
    # HINT: Mount ./app to /app
    # TODO: Configure service dependencies for the web service
    # HINT: Depends on redis
    # TODO: Configure networks for the web service
    # HINT: Use the default network or define a custom one

  redis:
    # TODO: Configure the Redis service
    # HINT: Use redis:6.2-alpine as the image
    # TODO: Configure ports for the Redis service
    # HINT: Map port 6379 to container port 6379
    # TODO: Configure volumes for the Redis service
    # HINT: Use a named volume for data

volumes:
  redis-data:

    # TODO: Define networks if needed
    # HINT: Uncomment and modify for custom network configuration
    #
    # networks:
    #   app-network:
    #     driver: bridge 
