FROM python:3.12-slim

# TODO: Configure security best practices for Docker containers
# HINT: Create a non-root user for running the application

# TODO: Set up the working directory for the container
# HINT: Create and use a dedicated directory

# TODO: Configure efficient layer caching for the build
# HINT: Copy and install dependencies before copying application code

# TODO: Configure the container to run as a non-root user
# HINT: Switch to the previously created user

# TODO: Set environment variables
# HINT: Configure the Python path and other necessary variables

# TODO: Define the command to start the application
# HINT: Use ENTRYPOINT for the main command